<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto" xmlns:android="http://schemas.android.com/apk/res/android">
    <data>
        <variable name="vm" type="com.tenacy.roadcapture.ui.MainViewModel"/>
        <variable name="childVm" type="com.tenacy.roadcapture.ui.AppInfoViewModel"/>
    </data>

    <LinearLayout android:id="@+id/ll_main_container"
                  android:layout_width="match_parent"
                  android:layout_height="match_parent"
                  android:orientation="vertical"
                  app:enabled="@{!childVm.signingIn}">

        <androidx.viewpager2.widget.ViewPager2
                android:id="@+id/viewPager"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"/>

        <com.tenacy.roadcapture.ui.CustomBottomNavigation
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:id="@+id/bottom_nav"
                android:outlineProvider="bounds"
                android:elevation="8dp"
                app:onNewItemClick="@{() -> vm.onNewClick()}"/>
    </LinearLayout>
</layout>