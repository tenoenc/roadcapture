<?xml version="1.0" encoding="utf-8"?>
<layout>

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
                                                       xmlns:app="http://schemas.android.com/apk/res-auto"
                                                       android:layout_width="wrap_content"
                                                       android:layout_height="wrap_content">

        <ImageView android:layout_width="50dp"
                   android:layout_height="50dp"
                   android:id="@+id/img_marker"
                   android:layout_marginTop="6dp"
                   app:layout_constraintTop_toTopOf="@id/view_marker"
                   app:layout_constraintStart_toStartOf="@id/view_marker"
                   app:layout_constraintEnd_toEndOf="@id/view_marker"/>

        <ImageView android:layout_width="64dp"
              android:layout_height="73dp"
              android:id="@+id/view_marker"
              android:src="@drawable/marker"
              app:layout_constraintTop_toTopOf="parent"
              app:layout_constraintStart_toStartOf="parent"
              app:layout_constraintEnd_toEndOf="parent"
              app:layout_constraintBottom_toBottomOf="parent"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
